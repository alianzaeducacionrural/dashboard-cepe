{
  "name": "ucampo-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend en Google Apps Script (via clasp) para el seguimiento de La Universidad en el Campo - CEPE.",
  "scripts": {
    "login": "clasp login",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "deploy": "clasp deploy --description \"v1\"",
    "open": "clasp open"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
